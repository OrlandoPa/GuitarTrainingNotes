package com.example.guitareartraining.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Palette ──
val Amber = Color(0xFFFFC107)
val AmberDark = Color(0xFFFF8F00)
val AmberLight = Color(0xFFFFE082)

// ── Accent / Feedback ──
val SuccessGreen = Color(0xFF66BB6A)
val SuccessGreenDark = Color(0xFF388E3C)
val ErrorRed = Color(0xFFEF5350)
val ErrorRedDark = Color(0xFFC62828)
val WarningOrange = Color(0xFFFFA726)

// ── Neutrals (Dark Theme) ──
val DarkBackground = Color(0xFF0D1117)
val DarkSurface = Color(0xFF161B22)
val DarkCard = Color(0xFF1C2333)
val DarkCardElevated = Color(0xFF243044)

// ── Text ──
val TextPrimary = Color(0xFFF0F6FC)
val TextSecondary = Color(0xFF8B949E)
val TextMuted = Color(0xFF484F58)

// ── Decorative ──
val GuitarStringColor = Color(0x33FFC107)
val GlowAmber = Color(0x40FFC107)

// Legacy aliases so existing references still compile
val PrimaryGreen = SuccessGreen
